apiVersion: v1
kind: ConfigMap
metadata:
  name: fb-config
data:
  fluent-bit.conf: |
    [SERVICE]
        Flush        5
        Log_Level    info
        Parsers_File parsers.conf

    [INPUT]
        Name         tail
        Tag          kube.*
        Path         /var/log/containers/*.log
        # Fluent Bit 自身のログを除外し、読み取り→出力→再読み取りの無限ループを防止
        Exclude_Path /var/log/containers/*log-collector*.log
        Parser       docker
        Refresh_Interval 10
        Read_From_Head On

    [FILTER]
        Name             kubernetes
        Match            kube.*
        Kube_URL         https://kubernetes.default.svc:443
        Kube_CA_File     /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        Kube_Token_File  /var/run/secrets/kubernetes.io/serviceaccount/token
        Merge_Log        On
        K8S-Logging.Parser On

    [OUTPUT]
        Name         stdout
        Match        *
        Format       json_lines

  parsers.conf: |
    [PARSER]
        Name         docker
        Format       json
        Time_Key     time
        Time_Format  %Y-%m-%dT%H:%M:%S.%L%z
