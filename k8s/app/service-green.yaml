apiVersion: v1
kind: Service
metadata:
  # OrbStack: http://green.hello-k8s-logging.svc.cluster.local:8080
  name: green
spec:
  type: NodePort
  selector:
    app: hello-k8s-logging
    variant: green
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 30081
