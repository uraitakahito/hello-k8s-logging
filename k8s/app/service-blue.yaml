apiVersion: v1
kind: Service
metadata:
  # OrbStack: http://hello-k8s-logging-blue.default.svc.cluster.local:8080
  name: hello-k8s-logging-blue
spec:
  type: NodePort
  selector:
    app: hello-k8s-logging
    variant: blue
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 30080
