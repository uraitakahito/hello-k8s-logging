apiVersion: v1
kind: ConfigMap
metadata:
  name: fb-config
data:
  fluent-bit.conf: |
    [SERVICE]
        Flush        5
        Log_Level    info

    [INPUT]
        Name         tail
        Tag          nginx.access
        Path         /var/log/nginx/access.log
        # Fluent Bit の起動が nginx より遅れても、起動前に書き込まれたログを取りこぼさない
        Read_From_Head On

    [INPUT]
        Name         tail
        Tag          nginx.error
        Path         /var/log/nginx/error.log
        Read_From_Head On

    [OUTPUT]
        Name         stdout
        Match        *
        Format       json_lines
