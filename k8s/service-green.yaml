apiVersion: v1
kind: Service
metadata:
  # OrbStack: http://hello-k8s-logging-green.default.svc.cluster.local:8080
  name: hello-k8s-logging-green
spec:
  type: NodePort
  selector:
    app: hello-k8s-logging
    variant: green
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 30081
